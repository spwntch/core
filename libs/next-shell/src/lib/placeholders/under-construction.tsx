'use client';
import { UnderConstruction as SpwnUnderConstruction } from '@/react-shell';
import { PropsWithChildren } from 'react';

export const UnderConstruction = (props: PropsWithChildren) => {
  return <SpwnUnderConstruction {...props} />;
};
