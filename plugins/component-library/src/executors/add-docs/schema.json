{
  "$schema": "https://json-schema.org/schema",
  "version": 2,
  "title": "AddDocs executor",
  "description": "",
  "type": "object",
  "properties": {
    "component": {
      "type": "string",
      "description": "The name of the component directory where the component is located.",
      "x-prompt": "What is the component directory name where your component is located?",
      "default": "lib/components/..."
    },
    "addTests": {
      "type": "boolean",
      "description": "Add storybook play specs for the component.",
      "default": true,
      "x-prompt": "Would you like to add tests for the component?"
    }
  },
  "required": ["component"]
}
