{
  "$schema": "https://json-schema.org/schema",
  "$id": "CreateLibrary",
  "title": "",
  "type": "object",
  "properties": {
    "directory": {
      "type": "string",
      "description": "Relative to your workspace root, where should we put this library (directory only)?",
      "alias": "d"
    },
    "name": {
      "type": "string",
      "description": "What shall we name this component library?",
      "default": "ui",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What shall we name this component library?",
      "alias": "n"
    },
    "publishable": {
      "type": "boolean",
      "description": "Will this library be published to npm?",
      "default": false,
      "x-prompt": "Will this library be published to npm?",
      "alias": "p"
    }
  },
  "required": ["name"]
}
