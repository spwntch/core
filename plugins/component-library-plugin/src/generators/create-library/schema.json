{
  "$schema": "https://json-schema.org/schema",
  "$id": "CreateLibrary",
  "title": "",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "What shall we name this component library?",
      "$default": {
        "$source": "argv",
        "index": 1
      },
      "x-prompt": "What shall we name this component library?"
    },
    "publishable": {
      "type": "boolean",
      "description": "Should this library be published to npm?",
      "$default": {
        "$source": "argv",
        "index": 2,
        "value": false
      },
      "x-prompt": "Should this library be published to npm?"
    },
    "addStories": {
      "type": "boolean",
      "description": "Does your workspace support Storybook? If so, should we add stories to the library for your first component?",
      "$default": {
        "$source": "argv",
        "index": 3,
        "value": false
      },
      "x-prompt": "Does your workspace support Storybook? If so, should we add stories to the library for your first component?"
    }
  },
  "required": ["name", "directory"]
}
