{
  "$schema": "https://json-schema.org/schema",
  "$id": "CreateLibrary",
  "title": "",
  "type": "object",
  "properties": {
    "directory": {
      "type": "string",
      "description": "Relative to your workspace root, where should we put this library (directory only)?",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "Relative to your workspace root, where should we put this library (directory only)?"
    },
    "name": {
      "type": "string",
      "description": "What shall we name this component library?",
      "$default": {
        "$source": "argv",
        "index": 2
      },
      "x-prompt": "What shall we name this component library?"
    },
    "publishable": {
      "type": "boolean",
      "description": "Should this library be published to npm?",
      "$default": {
        "$source": "argv",
        "index": 2,
        "value": false
      },
      "x-prompt": "Should this library be published to npm?"
    },
    "addStories": {
      "type": "boolean",
      "description": "Does your workspace support Storybook? If so, add stories to the library for your first component.",
      "$default": {
        "$source": "argv",
        "index": 3,
        "value": false
      },
      "x-prompt": "Does your workspace support Storybook? If so, add stories to the library for your first component."
    }
  },
  "required": ["name", "directory"]
}
